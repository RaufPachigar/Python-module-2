#What is used to check whether an object o is an instance of class A?

# The function isinstance(o, A) is used to check if an object o is an instance of the class A.

# Example:

if isinstance(rect, Rectangle):
    print("rect is an instance of Rectangle")